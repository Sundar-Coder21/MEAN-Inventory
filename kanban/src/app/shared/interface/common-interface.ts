export interface NavigationItem {
    text: string;
    path: string;
    icon: string;
}
export interface InventoryItem {
    id: number;
    name: string;
    description: string;
    quantity: number;
    unitPrice: number;
}